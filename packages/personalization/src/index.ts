/**
 * @fetch/personalization — prompt builder + guardrails + generation. Turns a
 * campaign template plus a lead's enriched context into a per-lead draft that a
 * human previews and approves before anything sends.
 */
export * from './bind';
export * from './guardrails';
export * from './generate';
