{
  "name": "@fetch/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fetch MCP server — a thin adapter over the Fetch HTTP API so an external AI can operate a Fetch workspace.",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
